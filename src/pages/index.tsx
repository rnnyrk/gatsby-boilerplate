import React from 'react';
import { graphql } from 'gatsby';

import { Template } from 'common/layout';

const Home = () => {
  return (
    <Template>
      Home
    </Template>
  );
};

Home.propTypes = {
};

export default Home;
