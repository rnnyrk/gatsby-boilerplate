import React from 'react';
import { graphql } from 'gatsby';

import { Template } from 'common/layout';

const Page = () => {
  return (
    <Template>
      <h1>Default page</h1>
    </Template>
  );
}

export default Page;

